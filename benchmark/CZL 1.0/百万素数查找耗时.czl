int total = 0
int isprime(int n)
{
	int i, count = os.sqrt(n)
	for (i=2; i<=count; ++i)
		if (n%i == 0)
			return false
	total++
	return true
}

main
{
	for i in (0, 1000000)
		isprime(i)
	
	echo(total)
	echo(clock())
}
